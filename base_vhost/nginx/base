server {
    listen 80;
    listen [::]:80;
    server_name ${serverName} ${aliasName};
    root ${documentRoot};
    index index.html index.htm index.php #deleteifhttps
} #end (comment needed for script)